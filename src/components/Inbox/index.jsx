import React, { useState } from 'react';
import Card from '../Card';
import { Wrap, AntModal, Cards } from './style';

const Inbox = () => {
  const [isModalOpen, setIsModalOpen] = useState(false);

  return <>Inbox</>;
};

export default Inbox;
